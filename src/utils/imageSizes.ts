// Keep these values in sync with the `images` object in `next.config.js`
const imageSizes = {
  postSummary: "320px",
  halfDocumentation: "435px",
  documentation: "870px",
}

export default imageSizes
